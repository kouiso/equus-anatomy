name: PR Auto Setting

on:
  pull_request:
    types: [opened, reopened]

permissions:
  pull-requests: write

jobs:
  assign-author:
    name: Assign author to PR
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: technote-space/assign-author@9558557c5c4816f38bd06176fbc324ba14bb3160
